# Core Python dependencies for data preparation and lightweight local checks.
# On the remote GPU machine, install CUDA-enabled torch/torchaudio separately
# following the PyTorch instructions for that machine's CUDA driver.
PyYAML>=6.0
hydra-core>=1.3
pytest>=8.0
g2p_en>=2.1.0
numpy
pandas
pyarrow
soundfile
tqdm
pytorch-lightning>=2.5  # WeightAveraging callback for optional Stage II EMA
tensorboard
torchmetrics
transformers
huggingface_hub
openai-whisper
rich  # RichProgressBar / run summary for Stage II console output
rapidfuzz  # phoneme-level hard-negative distance recompute for Stage II prep
# Zipformer KWS fbank parity: pip install -e ".[icefall]"
# Optional dashboard backends (install on the training machine as needed):
#   pip install wandb
#   pip install trackio
